rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all documents for now
    // NOTE: This is for development purposes. You should secure your database
    // with more specific rules before going to production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
